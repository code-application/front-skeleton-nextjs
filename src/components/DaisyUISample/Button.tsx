import type React from "react";

const DaisyUIButton: React.FC = () => {
  return (
    <button type="button" className="btn btn-primary">
      DaisyUI Button
    </button>
  );
};

export default DaisyUIButton;
